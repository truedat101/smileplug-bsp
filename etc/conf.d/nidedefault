#
# Set ARGS to point to your initial Node.js app/service
#
# XXX This is a real problem - the server won't find it's local paths for static content if we use the
# full path
# ARGS="/root/plugmin-server/plugmin-server.js"
ARGS="listen . --host 0.0.0.0 --port 9023"
SERVERDIR="/root/spdist/tty.js"
DAEMON="/usr/bin/nide"
